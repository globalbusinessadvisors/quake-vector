[package]
name = "quake-vector-types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde-big-array = "0.5"
uuid = { version = "1", features = ["v4", "serde"] }
